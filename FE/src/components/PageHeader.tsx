// VoltHub — PageHeader (alias kanonik Opsi C).
// Implementasi tunggal ada di components/v2/PageHeader (title + description +
// actions + optional backTo, gaya flat border-bottom); file ini re-export agar
// path impor `@/components/PageHeader` valid untuk komponen non-V2.
export { PageHeader } from "@/components/v2/PageHeader";
